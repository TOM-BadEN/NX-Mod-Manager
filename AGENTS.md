# 项目协作规则

本文件中的规则适用于本项目内的所有分析、实现和操作。遇到含义不明确或规则冲突的情况时，应采用更保守的处理方式：停止操作并向用户确认。

## 一、文件修改授权

- 默认只允许执行只读操作。
- 只有当用户在当前请求中明确说出“请修改”时，才允许创建、编辑、删除、移动或格式化项目文件。
- “分析一下”“检查一下”“看看”“讨论一下”“给出建议”等表述均不构成修改授权。
- 如果无法确定用户是否已经授权修改，必须先询问用户，不得自行修改。
- 获得修改授权后，只能修改当前任务明确涉及的内容，不得顺带修改无关代码或文件。

## 二、实现过程中的决策边界

- 开始实现前，应先确认已经讨论过的需求、实现范围和预期行为。
- 实现过程中，如果遇到未讨论、未确认或未提前考虑的问题，不得擅自选择实现方案。
- 此时必须停止实现，并向用户说明：
  - 遇到了什么问题；
  - 为什么需要作出新的决定；
  - 可选择的处理方案及其影响。
- 在用户明确选择方案之前，不得继续处理依赖该决定的内容。

## 三、代码风格与参考实现

- 修改或新增代码时，必须遵循项目现有的代码风格。
- 代码风格包括但不限于命名方式、注释风格、缩进方式、空行使用、换行方式、括号位置以及整体排版结构。
- 如果用户已经提供项目内的参考代码，必须先理解该参考代码的实现方式，再进行实现。
- 如果项目内已经存在功能相同或作用类似的代码，应优先将其作为实现参考。
- 在不违背用户明确需求的前提下，新实现应尽量与参考代码保持一致，包括：
  - 实现方法；
  - 代码组织方式；
  - 代码排版；
  - 注释样式；
  - 逻辑层级；
  - 文件及模块中的放置位置。
- 不得仅凭个人偏好引入与项目现有代码明显不同的实现风格。
- 如果现有参考代码之间存在明显冲突，或者无法判断应当遵循哪一种实现方式，必须停止实现并向用户说明。

## 四、`try` 语法限制

- 禁止在项目源代码中使用 `try` 异常处理语句。
- 该限制针对 `try` 异常处理语法结构，不针对注释、文档或标识符中出现的英文单词 `try`。
- 不得新增依赖 `try` 异常处理语句的实现。
- 如果当前任务无法在不使用 `try` 异常处理语句的情况下完成，必须停止实现并向用户说明，不得自行破例。
- 如果当前任务涉及的现有代码中已经使用了 `try` 异常处理语句，必须先向用户报告并等待指令，不得擅自决定保留、修改或删除。

## 五、编译限制

- 除非用户明确要求编译，否则禁止在任何时候编译本项目。
- 不得执行会直接或间接触发项目编译的命令、脚本或开发工具操作。
- 即使编译有助于验证修改，也必须先获得用户的明确授权。
- 未获得编译授权时，只能使用不会触发编译的只读检查方式。

## 六、回答与术语说明

- 回答用户时，如果需要使用英文技术术语、方法名、函数名、变量名或其他英文标识符，应在其第一次出现时，用简短中文说明它在本项目中的用途。
- 说明应紧邻该词语，避免要求用户自行猜测其含义。
- 同一个词语在同一次回答中再次出现时，通常不需要重复解释。

## 七、回答的完整性与简洁性

- 回答应优先提供结论和用户判断、核对所需的关键信息，避免重复背景、已确认内容和无关建议。
- 简洁不得以省略关键条件、风险、边界情况、设计决策、行为变化或验证结果为代价。
- 发现可能影响正确性、安全性、性能、兼容性、可维护性或用户数据的问题时，必须明确说明。
- 实现任务完成后，应说明实现方案、关键逻辑、修改范围和验证结果，确保用户能够核对实现，不得将操作过程变成黑箱。
- 不得逐行复述代码或罗列没有核对价值的常规操作步骤，除非用户明确要求。

## 八、框架源码修改限制

- 在分析、设计或实现任何方案时，禁止提出或采用修改框架源码的方案。
- 所有方案必须限定在项目自身代码范围内。
- 如果当前需求无法在不修改框架源码的情况下完成，必须停止并向用户说明，不得提出或实施框架源码修改。
